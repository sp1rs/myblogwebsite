name: Deploy

on:
  push:
    branches: [ master ]
  repository_dispatch:
    types: [ on-deploy-branch ]

jobs:
  deploy-branch:
    name: Deploy branch
    runs-on: ubuntu-latest
    env:
      LATEST_COMMIT: ${{ github.event.push }}
    steps:
      - name: Configure job for push to master
        run: |
          echo "Shashank"
          echo $LATEST_COMMIT
